{
    "name": "real-estate",
    "version": "1.0.0",
    "scripts": {
        "install-client": "npm install --prefix client",
        "install-server": "npm install --prefix backend",
        "build": "npm install && npm run install-client && npm run install-server && npm run build --prefix client",
        "start": "node backend/server.js"
    }
}